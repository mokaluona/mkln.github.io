# 待解决问题

本站（<https://www.mkln.space>）已确认、尚未修复的问题清单。和 [CLAUDE.md](CLAUDE.md)、[写作说明.md](写作说明.md) 一样**不发布到网站**。

- 最近核实：**2026-09-13**（本地 `main` 已 `git pull` 同步至 `origin/main` = `d2fdcad`）
- 状态标记：⬜ 待处理　🟡 等你定方案　✅ 已完成

## 一览

| # | 问题 | 影响面 | 状态 |
|---|---|---|---|
| 1 | 文章页「相关文章」的配图全是坏图 | 每篇文章页 4 处 + 站内搜索配图，肉眼可见 | 🟡 |
| 2 | 段首缩进量是 1.3em，不是严格意义的「两格」 | 全站正文观感 | 🟡 |
| 3 | Font Awesome 走 `@latest`（现解析到 7.3.1） | 全站图标，潜在 | ⬜ |
| 4 | `about.md` 末尾有一段从来没生效的样式 | 无（死代码） | ⬜ |
| 5 | `_config.yml` 的 `font_awesome: true` 是死配置 | 无（误导） | ⬜ |
| 6 | RSS（`feed.xml`）不跟自定义摘要标记走 | 订阅器里读到的摘要 | ⬜ |
| 7 | 主题残留文件被发布到了网站根目录 | 站根目录多出 3 个文件 | ⬜ |
| 8 | `friends.md` 的样式与结构有硬伤（无效 CSS、畸形 HTML） | 友链页观感 | ✅ |
| 9 | 一篇文的文件名日期与 front matter `date` 不一致 | 列表排序 | 🟡 |

---

## 1. 文章页「相关文章」的配图全是坏图 🟡

**现象**：每篇文章页面底部「相关文章」的 4 张卡片，图片位置显示的是破图。

**证据**：线上文章页 HTML 里出现 4 次 `<img src="/attachments/image" alt="">`（我实测抓的线上页面）。`/attachments/image` 是一个**目录**，不是图片文件。

**原因**：

- `_config.yml` 第 28 行 `teaser: "/attachments/image"` —— 指向了目录。
- `_includes/archive-single.html` 第 1–5 行：文章自己没有 `post.header.teaser` 时，回落到 `site.teaser`（就是上面这个目录）。
- `_includes/page__related.html` 第 8 行用 `type="grid"` 渲染，而只有 grid 类型才会输出配图。
- 现有 7 篇文章的 front matter 里**都没有** `header.teaser`，所以 4 张卡片全部落到这个目录路径上。

**为什么列表页没事**：首页、`/blog/`、分类页、标签页都用 `type="list"`，list 不输出配图。

**影响还不止相关文章**：站内搜索也会踩同一个坑——`assets/js/lunr/lunr-store.js` 第 12–16 行同样把没有 `header.teaser` 的条目回落到 `site.teaser`，`assets/js/lunr/lunr-en.js` 第 45–54 行会在搜索结果里渲染这张图。所以 `/search/` 搜出文章时，配图位置同样是坏图。

**可选方案**：

- **A（建议）**：删掉 `_config.yml` 里的 `teaser` 行或把它留空 → 卡片不再出图，只显示标题和摘要。既然现在没有一篇文章配了图，这样最干净。
- **B**：指向一张真实图片（比如 `/attachments/image/head-picture.jpg`）→ 4 张卡片共用同一张图，观感一般。
- **C**：给每篇文章单独加 `header.teaser` → 最像样，但要逐篇找图，工作量大。

---

## 2. 段首缩进的量：1.3em 还是 2em 🟡

**缩进外溢的 bug 已经修好了**（提交 `bfcb56f`）——原先挂在全局 `p` 上的 `text-indent` 已经收窄到正文容器，阅读时长、标签、分类、更新日期、列表摘要这些非正文段落不再被缩进。

**但缩进量沿用原值 1.3em**，严格意义的「两格」是 `2em`。这个只能你来定，因为它是观感问题。

改法：`_sass/minimal-mistakes/_base.scss` 第 89 行，把 `1.3em` 换成 `2em`。

⚠️ **不要动 `$indent-var`**（那两处是 `1.3em`），它同时控制段间距，改了会连带改变段落之间的空隙。

---

## 3. Font Awesome 走 `@latest` ⬜

**位置**：`_includes/head.html` 第 21–22 行（preload 与 noscript 两处），引用的是
`https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@latest/css/all.min.css`。

**现状**：`@latest` 现在解析到 **Font Awesome 7.3.1**，而主题是按 FA5 的类名写的（`far` / `fas` / `fab`、`fa-clock`、`fa-calendar-alt`）。

**我核实过**：7.3.1 的 CSS 里这些旧别名**仍然存在**（`fa-clock` → `\f017`、`fa-calendar-alt` → `\f073`、`.far` 与 `.fas` 家族类都在），所以**图标现在正常显示**。这不是当前故障，是隐患。

**风险**：哪次大版本把旧别名删掉，全站图标会**一起失效**，而且不报错——只是全部变成空白方块或问号，很难第一时间联想到 CDN。

**修法**：把那两处 `@latest` 钉成 `6.x`（FA5 别名齐全且长期维护）或钉成已验证过的 `7.3.1`。一次性、低风险。

---

## 4. `about.md` 末尾有一段从来没生效的样式 ⬜

**位置**：`about.md` 第 62–67 行：

```html
<style>
.post-content p {
  text-indent: 2em;
  margin: 0 0 1em 0;
}
</style>
```

**问题**：`.post-content` 这个类名**在整份仓库里只出现在这一处**，没有任何元素带它（本站正文容器叫 `.page__content`）。所以这段样式从写下的那天起就没起过任何作用。

**顺带说明**：about 页现在的段首缩进来自第 2 条里那条新的正文规则（1.3em）。

**处理**：删掉这段死代码；或者如果你本来就想让 about 页用 2em，改写成正确的选择器。这一条和第 2 条联动，可以一起定。

---

## 5. `_config.yml` 的 `font_awesome: true` 是死配置 ⬜

**位置**：`_config.yml` 第 341 行（上面第 340 行是注释 `# 打开font_awesome图片`）。

**问题**：`_includes/head.html` 是**无条件**加载 Font Awesome 的，仓库里没有任何地方读 `site.font_awesome`。这行配置唯一的实际效果是让人误以为「关掉它就不加载图标了」。

**处理**：要么删掉，要么真的把它接成开关（用 `{% if site.font_awesome %}` 把 head.html 里那两行 link 包起来）。

---

## 6. RSS（`feed.xml`）不跟自定义摘要标记走 ⬜

**问题**：`<!--excerpt-->` / `<!--more-->` 标记目前只管两处——列表页摘要和网页描述。RSS 订阅器里读到的仍然是 Jekyll 原生摘要（首段）。

**原因**：`feed.xml` 是 jekyll-feed 插件生成的，仓库里根本没有这个文件，所以用不上 `_includes/excerpt-custom.html`。

**要接管的话需要**：自建 `feed.xml` + 让 jekyll-feed 不再生成（改 `atom_feed` 配置，或把它从 `plugins` 里去掉），然后验证 GitHub Pages 是否允许这样覆盖。中等工作量，且要实测。

**先确认要不要做**。如果你自己不读 RSS、订阅者也少，可以先放着。

---

## 7. 主题残留文件被发布到了网站根目录 ⬜

根目录这三个文件不在 `_config.yml` 的 `exclude` 里，所以被一起发布了（我实测都返回 200，可以直接打开）：

| 文件 | 大小 | 是什么 |
|---|---|---|
| `screenshot.png` | 206 KB | Minimal Mistakes 主题的宣传截图 |
| `screenshot-layouts.png` | 122 KB | 同上 |
| `staticman.yml` | 3.7 KB | Staticman 评论的示例配置；本站 `comments.provider` 是 `"false"`，用不上 |

都不属于你的站点内容，任何人拼 URL 都能看到。

**处理**：直接删掉，或加进 exclude。

### 顺手的小清理（非问题）

- `_config.yml` 第 130 行注释里留着 `url: "https://mkln.online.com"`——域名本身就是错的（`.online.com`）。它在注释里不影响运行，但容易误导，建议连同那几行注释一起清掉。
- `_includes/head.html` 第 9 行有一行 Google Slides 的注释链接（主题作者留下的），可删可留。

---

## 8. `friends.md` 的样式与结构有硬伤 ✅

> 已在提交 `49fb0f2` 随整页重构一并解决（去内联样式、改扁平卡片、清理畸形 `<ul>`）。下面两条硬伤记录备查。

`about.md` 里你已经说过友链页是 vibe coding、想重写。这里补两条**具体的、可验证的**硬伤（不只是「观感一般」）：

1. **一条 CSS 是无效的**：`friends.md` 末尾 `<style>` 里 `.friend-intro` 的
   `border-left: 4px solidrgb(99, 76, 175);` —— `solid` 和 `rgb(` 之间**缺了一个空格**，被浏览器拼成不认识的 `solidrgb`，整条 `border-left` 直接丢弃。所以 `.friend-intro` 左边那条紫色竖线从来没显示过。
2. **「想要交换友链」那段是畸形的嵌套 `<ul>`**：连续多层 `<ul>` 互相嵌套，中间夹着 `<h4>` 和 `<p>`，却没有一个 `<li>`。HTML 上非法，浏览器只能靠容错硬扛，渲染出来是一层层缩进的空列表。

这两条都是「删/改」级的小事，也可以留到你重写友链页时一起处理。

---

## 9. 一篇文的文件名日期与 front matter `date` 不一致 🟡

`_posts/2026-6-6-服装、建筑与历史连续性：观上海世博会博物馆有感.md`：

- 文件名日期：`2026-6-6`
- front matter：`date: 2026-01-18`，`last_modified_at: 2026-06-06`

Jekyll 排序按 front matter 的 `date` 走，所以这篇现在排在 **2026-01-18**，在列表里会出现在 4 月的练笔**之前**。如果本意是「6 月发的文」，`date` 应是 `2026-06-06`；如果是「1 月参观、6 月整理」，那现状就是对的。永久链接是 `/:categories/:title/`，不受影响，只有排序受影响。**这一条要你确认**是不是笔误。

---

## 已完成（备查）

| 提交 | 内容 |
|---|---|
| `fa1c595` | 修复站点配置错误，并新增自定义摘要标记功能 |
| `5d5db83` | 新增 [写作说明.md](写作说明.md) 与 [CLAUDE.md](CLAUDE.md)，并让自定义摘要脱离 160 字截断 |
| `0dbc70e` | 文档补充：分支与发布流程 |
| `bfcb56f` | **修复段首缩进外溢**：全局 `p` 的 `text-indent` 收窄到正文（阅读时长/标签/分类不再重叠） |
| `b7b683b` | `CLAUDE.md` 记录正文段首缩进的实现与 `text-indent` 继承陷阱 |
| `49fb0f2` | **重构友情链接页**：去内联 AI 味样式，改主题变量 + 扁平卡片，清理畸形 `<ul>` |

> 发布流程见 [写作说明.md](写作说明.md) 末尾「发布流程」一节，或 [CLAUDE.md](CLAUDE.md) 的「分支与发布」。
