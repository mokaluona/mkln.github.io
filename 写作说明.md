# 写作说明

这篇讲文章摘要怎么写。给 AI 看的维护说明在 [CLAUDE.md](CLAUDE.md)。

## 一句话版本

**什么都不写** → 和以前一样，列表里显示你文章的第一段（最多 160 字）。

**写了标记** → 你想让摘要是什么，它就是什么。

## 两种标记

### 1. `<!--excerpt-->` 和 `<!--/excerpt-->` —— 最灵活

把想当摘要的内容夹在这两个标记中间。**放在哪里都行，包括句子中间。**

```
今天去看了《控方证人》。<!--excerpt-->一部 1957 年的黑白片，几乎全部场景都在同一间法庭里，却靠台词和信息密度把人钉在座位上。<!--/excerpt-->还未观看并且有观影意愿的不建议阅读哦。
```

列表里显示的摘要就是"一部 1957 年的黑白片……把人钉在座位上。"这一句。文章正文一个字都没少——标记本身是 HTML 注释，网页上不显示。

### 2. `<!--more-->` —— 常见写法

标记**之前**的内容全部当摘要，之后的归正文。

```
第一段写点什么。

第二段也想放进摘要。

<!--more-->

从这里开始就不进摘要了。
```

注意：因为这个标记之前的**全部**内容都算摘要，所以上面例子里摘要会是前两段，而不再是第一段。

## 优先级

1. `<!--excerpt-->` … `<!--/excerpt-->` 之间的内容
2. `<!--more-->` 之前的内容
3. 都没写 → 第一段（原来的默认行为）

两种都写的时候，`excerpt` 标记赢。

小细节：如果一篇文章里写了**多组** `<!--excerpt-->`，只有**最后一组**生效。正常情况下别写多组就好。

## 摘要出现在哪儿，效果有什么不同

| 出现位置 | 写了标记 | 没写标记 |
|---|---|---|
| 列表页摘要（首页 / `/blog/` / 分类页 / 标签页 / 文章底部的相关文章） | 你写的内容，**不截断** | 第一段，**最多 160 字符** |
| 网页描述（搜索结果、微信/社交平台分享卡片上显示的描述） | 你写的内容 | 第一段 |
| RSS 订阅（`feed.xml`） | **不生效**，仍是第一段 | 第一段 |

三件要留意的事：

**160 字符上限只对没写标记的文章生效。** 写了标记，你写多长就显示多长。但那毕竟是列表，摘太长会把版面撑开，建议控制在两三行以内。另外这个 160 是**含结尾省略号在内**的总长度，实际可见文字约 157 字。

**front matter 里的 `description` 优先级最高**，会盖过标记。如果你在文章开头写了：

```
---
title: "《控方证人》影评"
description: "这是我手动写的描述"
---
```

那么**网页描述**会显示"这是我手动写的描述"，标记不起作用。但**列表页摘要不受 `description` 影响**，仍然按标记走。这个不对称是主题原本的行为，没有改过。

**RSS 摘要不跟着标记走。** 订阅器里读到的是 Jekyll 自己的首段摘要。如果你在意这一点，告诉小白，需要另外接一下。

## 完整例子

```
---
title: "《控方证人》影评"
categories:
  - 文章
date: 2025-12-05
---

<!-- 删减自【2025.12.5】日记 -->

今天去看了影史经典《控方证人》，简单写个观后感。<!--excerpt-->一部 1957 年的黑白片，几乎全部场景都在同一间法庭里，却靠台词和信息密度把人钉在座位上。<!--/excerpt-->还未观看并且有观影意愿的不建议阅读哦，No spoiler！

后面是正文……
```

- **列表页摘要**："一部 1957 年的黑白片，几乎全部场景都在同一间法庭里，却靠台词和信息密度把人钉在座位上。"
- **网页描述**：同一句
- **正文**：从"删减自……"到结尾，一个字都没少

## 常用场景对照

| 我想要的效果 | 怎么写 |
|---|---|
| 摘要就是第一段（不变） | 什么都不写 |
| 摘要 = 前两段 | 在第二段之后加 `<!--more-->` |
| 摘要从段落中间截取 | 用 `<!--excerpt-->` … `<!--/excerpt-->` 把那一句夹起来 |
| 摘要跨好几段，但后面还有正文 | 用 `<!--excerpt-->` … `<!--/excerpt-->` 把这几段夹起来 |
| 摘要和正文完全不同的文字 | 用 `<!--excerpt-->` … `<!--/excerpt-->`（注意这段字会留在正文里，见下） |

## 常见坑

- 标记必须**一字不差**、大小写敏感。`<!--excerpt -->`（多一个空格）、`<!---more--->` 都不行。
- 两个标记的**顺序**别写反：先 `<!--excerpt-->`、后 `<!--/excerpt-->`。写反了不生效。
- 标记是 HTML 注释，**不会出现在网页上**，放心放在正文里。
- **标记内的文字仍然留在正文中**，不是"切走"。所以别把不想让读者看到的私人内容放进 `<!--excerpt-->` 里——它既会当摘要，也会留在正文。
- 想让摘要"到此为止"，用 `<!--more-->`；想"从段中取一句"，用 `<!--excerpt-->`。

## 想改规则的话

这些行为定义在 `_includes/excerpt-custom.html`（解析优先级）和 `_includes/archive-single.html`（160 字截断规则）里。改之前记住一条硬约束：**GitHub Pages 不允许自定义 Ruby 插件，只能用 Liquid**。
