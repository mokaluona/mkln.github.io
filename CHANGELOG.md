# 变更记录

本站（<https://www.mkln.space>）**站点层面**的改动记录——主题、样式、配置、功能、文档、脚本。**不发布到网站**（和 `CLAUDE.md`、`写作说明.md`、`待解决问题.md` 一样，见 `_config.yml` 的 `exclude`）。

文章本身的增删改**不在本表**——那是内容，不是站点改动。

> 约定：倒序，最新在上；每条带提交 hash，方便溯源；回退（revert）也记，避免「改了又退」看不明白。

## 2026-09-14

- **友链页整体重构**（合并 `c127045`，此前一次重构已回退，这是重做版）：`friends.md` 去掉 300+ 行内联 `<style>`（渐变 / 阴影 / 紫色，AI 味），改为竖排卡片；样式收敛进 `_sass/minimal-mistakes/_friends.scss`，全部复用主题变量；`_data/friends.yml` 清掉示例占位，只留 3 条真实友链。
  - 三个样式修复：卡片每行最多 2 张（`c368932`）；友链名访问后不再变蓝（`975eb31`，补 `:visited`）；点击后不出现红色焦点框（`535b0b8`）。
  - 卡片边框：默认色先加深为原 hover 色、hover 再深一档（`679d8aa`），随后默认色逐次调淡为 20%（`99ff46e`）→ 10%（`7ef1835`）。
  - **手机端卡片布局反复了三轮，最终回到单列横排**：
    1. 单列横排——头像在左，站名与简介在右（`2c94adf`）。
    2. 改为恒定两列小卡（`4a81fd0`）——卡片被拉得又高又空、边框看着像没封口，废弃。
    3. **回到单列横排**：日期由简介下方改为**靠右下角**（`.friend-date { justify-self: end }`，桌面用 `$small` 断点覆盖回左对齐）；卡片上下内边距 `1em` → `1.5em`。桌面端两列布局未动。
       - 日期再往左挪 11px（`margin-right: 1em`），不再贴着内容右边缘。
  - `.friend-header` 在重构时已移除（卡片改为直接子元素，`grid-template-areas` 才认得）。
- 文档：`待解决问题.md` 第 8 条（友链页硬伤）标记已完成（`487fd7b`）。

## 2026-09-13

- 新增 `待解决问题.md` 待办清单并排除发布（`82a26d6`），随后补充搜索配图影响、友链页硬伤、文件名日期不一致等（`61ce9d9`）。
- Obsidian 配置整目录忽略（`38e132b`、`0a7aace`、`3f317d3`）：`_posts/.obsidian/` 里嵌着 Google OAuth 密钥，公开仓库必须**整目录**忽略，不能只挑文件排除。
- **修复段首缩进外溢**（`bfcb56f`）：全局 `p` 的 `text-indent` 收窄到正文容器，阅读时长 / 标签 / 分类 / 更新日期不再被缩进。
- 文档：`CLAUDE.md` 记录段首缩进的实现与 `text-indent` 继承陷阱（`b7b683b`）。
- 文档：补充分支与发布流程（`0dbc70e`）。
- 新增 `写作说明.md` 与 `CLAUDE.md`，并让自定义摘要脱离 160 字截断（`5d5db83`）。
- **修复站点配置错误，新增自定义摘要标记**（`fa1c595`）：`<!--excerpt-->` / `<!--more-->` 摘要机制（`_includes/excerpt-custom.html`）。

## 2026-04

- 域名迁移到 `mkln.space`：`CNAME` 更新（`5b43c2d`、`315a648`、`c9d51c4`）。
- `about.md` 新增「迁站的话」（`2cbf172`）。
- 调整友链页显示颜色（`d94aa07`、`fc07282`）。

## 2025

- 首次部署上线（`6c92d1c`，07-25）。
- 建立 `CNAME`（`4746734`、`e46afe3`）。
- 陆续添加友链（`ad238a8`、`8507f0f`、`d15d591` 等）。

## 维护约定

- 每次改完站点代码 / 样式 / 配置，顺手在这里补一条（一句话要点 + 提交 hash）。
- 只记站点改动，不记文章增改。
- 回退（revert / 重做）也记，标清楚「这是对哪次改动的撤销」。
